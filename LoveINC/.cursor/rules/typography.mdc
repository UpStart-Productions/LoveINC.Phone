---
description: Mobile app typography – use vars and global classes, never hardcode
globs:
  - src/**/*.scss
  - src/**/*.html
  - packages/**/*.scss
alwaysApply: false
---

# Mobile App Typography

**CRITICAL:** Do NOT hardcode font sizes. Use CSS variables or typography utility classes.

## Text Sizes (5 tokens – use `var(--app-font-size-*)`)

- `--app-font-size-xs` (0.75rem) – CTA subtext, fine print
- `--app-font-size-sm` (0.875rem) – captions, pills, secondary
- `--app-font-size-body` (1rem) – default body
- `--app-font-size-title` (1.25rem) – card/list titles
- `--app-font-size-display` (1.5rem) – page titles, hero

## Icon Sizes (4 tokens – use `var(--app-icon-size-*)`)

- `--app-icon-size-sm` (0.875rem)
- `--app-icon-size-md` (1.25rem)
- `--app-icon-size-lg` (1.5rem)
- `--app-icon-size-xl` (2rem)

## Typography Classes (prefer these)

- `.app-title`, `.app-body`, `.app-body-secondary`, `.app-body-sm`, `.app-body-secondary-sm`, `.app-body-secondary-xs`
- `.app-caption`, `.app-large`, `.app-display`, `.app-display-lg`, `.app-link`

## Rules

1. **Never** use `font-size`, `font-weight`, `color`, or `line-height` for text in component SCSS. Use typography classes in HTML instead.
2. **Never** use `font-size: Xrem` or `font-size: Xpx` in component styles. Use vars only when a class cannot apply (e.g. icon sizing).
3. **Never** add new font-size variables without updating `docs/TYPOGRAPHY-GUIDE.md`.
4. Use **weight and color** for hierarchy, not more size steps.
5. **Before adding text styles** to any component: check if `.app-title`, `.app-body`, `.app-body-secondary`, `.app-body-secondary-sm`, `.app-body-secondary-xs`, `.app-caption`, `.app-large`, `.app-display`, `.app-display-lg`, or semantic HTML (h1–h6, p) already provides the style.

## Anti-Pattern (causes redundant styles)

```scss
// ❌ BAD – component-level text styles
.my-component-title {
  font-size: var(--app-font-size-display);
  font-weight: var(--app-font-weight-semibold);
  color: var(--app-text-primary);
}
```

```html
<!-- ✅ GOOD – use typography class in HTML -->
<h2 class="app-large">Title</h2>
```

## Reference

Full guide: `docs/TYPOGRAPHY-GUIDE.md`
